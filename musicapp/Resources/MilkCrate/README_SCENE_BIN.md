# Milk crate 3D (`scene.gltf` + `scene.bin`)

Bundled Sketchfab “Milk Crate” mesh (CC-BY-4.0 — see `license.txt`). `MilkCrateSceneController` loads the glTF for the crate shell only; saved discs are drawn in SwiftUI via `SavedCrateFrontStackView` / `FigmaVinylView`.
